#include "ConfigManager.h"
#include <iostream>
#include <stdexcept>

// ConfigManager implementation is already provided in the header file
// as it's a simple class with inline methods for brevity.
// If you need to add more complex functionality, you can expand this file.

// Example usage:
// int main() {
//     try {
//         ConfigManager config("config.txt");
//         std::string serverAddress = config.getConfig("server_address");
//         std::cout << "Server Address: " << serverAddress << std::endl;
//     } catch (const std::exception& e) {
//         std::cerr << "Error: " << e.what() << std::endl;
//     }
//     return 0;
// }
